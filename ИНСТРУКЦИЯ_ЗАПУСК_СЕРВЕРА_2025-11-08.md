# Инструкция по запуску локального веб-сервера
**Дата создания: 08.11.2025**

## 📋 Быстрый старт

### Шаг 1: Откройте Терминал на Mac

1. Нажмите `Cmd + Space` (или откройте Spotlight)
2. Введите "Terminal" и нажмите Enter
3. Или откройте через: **Приложения → Утилиты → Терминал**

### Шаг 2: Перейдите в папку проекта

В Терминале введите:

```bash
cd /Users/stanislav/web
```

Нажмите Enter.

**Проверка:** Вы должны увидеть в строке терминала что-то вроде:
```
stanislav@MacBook web %
```

### Шаг 3: Запустите сервер

Выберите один из вариантов ниже:

---

## 🐍 Вариант 1: Python HTTP сервер (рекомендуется)

### Команда:
```bash
python3 -m http.server 8000
```

### Что происходит:
- Запускается встроенный веб-сервер Python
- Сервер будет доступен на `http://localhost:8000`
- В терминале появится сообщение: `Serving HTTP on :: port 8000`

### Остановка сервера:
Нажмите `Ctrl + C` в терминале

---

## 🚀 Вариант 2: Использование готового скрипта

### Команда:
```bash
./start-server.sh
```

### Что происходит:
- Скрипт автоматически выберет подходящий способ запуска
- Сервер будет доступен на `http://localhost:8000`

### Остановка сервера:
Нажмите `Ctrl + C` в терминале

**Если скрипт не запускается:**
```bash
chmod +x start-server.sh
./start-server.sh
```

---

## 📦 Вариант 3: Node.js http-server

### Команда:
```bash
npx --yes http-server -p 8000 -c-1
```

### Что происходит:
- Устанавливается и запускается http-server (если не установлен)
- Сервер будет доступен на `http://localhost:8000`
- Флаг `-c-1` отключает кеширование для разработки

### Остановка сервера:
Нажмите `Ctrl + C` в терминале

---

## ✅ Проверка работы сервера

### Способ 1: Откройте в браузере на Mac

1. Откройте любой браузер (Safari, Chrome, Firefox)
2. Введите в адресной строке: `http://localhost:8000`
3. Должен открыться ваш сайт

### Способ 2: Проверка через curl

В новом окне терминала введите:
```bash
curl http://localhost:8000
```

Должен вернуться HTML-код страницы.

---

## 📱 Тестирование в iOS-симуляторе

### Шаг 1: Запустите локальный сервер
Следуйте инструкциям выше (Вариант 1, 2 или 3)

### Шаг 2: Откройте Xcode Simulator

1. Откройте **Xcode**
2. Выберите: **Xcode → Open Developer Tool → Simulator**
   - Или нажмите `Cmd + Space` и введите "Simulator"

### Шаг 3: Выберите устройство

1. В Simulator выберите: **File → Open Simulator → iPhone 15 Pro**
   - Или любое другое устройство из списка
2. Дождитесь загрузки симулятора

### Шаг 4: Откройте Safari в симуляторе

1. В симуляторе откройте приложение **Safari**
2. В адресной строке введите: `http://localhost:8000`
3. Нажмите Enter или "Go"
4. Сайт должен загрузиться

---

## 🔧 Если localhost не работает в симуляторе

### Используйте IP-адрес вашего Mac

#### Шаг 1: Узнайте IP-адрес Mac

Откройте новый терминал и введите:

```bash
ipconfig getifaddr en0
```

Или:

```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**Пример результата:** `192.168.1.100`

#### Шаг 2: Используйте IP вместо localhost

В Safari симулятора введите:
```
http://192.168.1.100:8000
```
(Замените на ваш IP-адрес)

---

## 🛑 Остановка сервера

### Способ 1: Если сервер запущен в терминале

1. Найдите окно терминала, где запущен сервер
2. Нажмите `Ctrl + C`
3. Сервер остановится

### Способ 2: Если забыли, где запущен сервер

Найдите и остановите процесс:

```bash
lsof -ti:8000 | xargs kill
```

Или:

```bash
killall python3
```

---

## 🔄 Изменение порта

Если порт 8000 занят, используйте другой порт:

### Python:
```bash
python3 -m http.server 3000
```

### Node.js:
```bash
npx --yes http-server -p 3000 -c-1
```

### Скрипт:
Отредактируйте файл `start-server.sh` и измените `PORT=8000` на нужный порт.

---

## ❓ Решение проблем

### Проблема: "Port 8000 is already in use"

**Решение:** Используйте другой порт (см. раздел "Изменение порта" выше)

Или найдите и остановите процесс, занимающий порт:
```bash
lsof -ti:8000 | xargs kill
```

### Проблема: "python3: command not found"

**Решение:** Python должен быть установлен на Mac по умолчанию. Проверьте:
```bash
which python3
```

Если не найден, установите через Homebrew:
```bash
brew install python3
```

### Проблема: "npx: command not found"

**Решение:** Установите Node.js с сайта https://nodejs.org/ или через Homebrew:
```bash
brew install node
```

### Проблема: Сайт не загружается в симуляторе

**Решение:**
1. Убедитесь, что сервер запущен (проверьте в браузере на Mac)
2. Используйте IP-адрес вместо localhost (см. раздел выше)
3. Проверьте, что порт правильный (8000)

### Проблема: "Permission denied" при запуске скрипта

**Решение:**
```bash
chmod +x start-server.sh
./start-server.sh
```

---

## 📝 Краткая шпаргалка

```bash
# 1. Перейти в папку проекта
cd /Users/stanislav/web

# 2. Запустить сервер (выберите один вариант)
python3 -m http.server 8000
# ИЛИ
./start-server.sh
# ИЛИ
npx --yes http-server -p 8000 -c-1

# 3. Открыть в браузере
# http://localhost:8000

# 4. Остановить сервер
# Ctrl + C в терминале
```

---

## 📚 Дополнительная информация

- **Файл проекта:** `/Users/stanislav/web`
- **Порт по умолчанию:** `8000`
- **Локальный адрес:** `http://localhost:8000`
- **Скрипт запуска:** `start-server.sh`

---

**Примечание:** Эта инструкция создана 08.11.2025. Если у вас возникли вопросы или проблемы, проверьте актуальность инструкции.

